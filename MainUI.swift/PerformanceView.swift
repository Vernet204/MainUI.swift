import SwiftUI

struct PerformanceView: View {

    var body: some View {

        VStack(spacing: 20) {

            Text("Company Performance")
                .font(.largeTitle)

            Text("Weekly Revenue")
            Text("Monthly Revenue")
            Text("Top Commodity")

            Spacer()
        }
        .padding()
    }
}
